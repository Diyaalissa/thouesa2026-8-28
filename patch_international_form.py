import sys

with open('src/components/sender/SenderPortal.tsx', 'r') as f:
    content = f.read()

# For storeUrl we can add an image URL next to it, and for specs we can use a large text area.
# Wait, let's see what is currently in storeItems block.
